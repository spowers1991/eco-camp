import React from 'react';
import Heading from "@/components/html_tags/Heading";

const RelatedListings = ({ story }) => {
  return ( 
        <div className='flex flex-row gap-6 pt-0 sm:pt-2'>
            test
        </div>
  );
};

export default RelatedListings;
